# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
todoテーブルの全データを取得するとTodoモデルの配列として返される

### Todoモデルのallメソッドの返り値は何か
illminate\Database\Eloquent\Collectionクラスのインスタンス

### 配列の代わりにCollectionクラスを使用するメリットは
メソッドチェーンが操作できる

### view関数の第1・第2引数の指定と何をしているか
第１引数　画面に表示したいbladeファイル
第２引数　渡したいデータの連想配列　[blade内での変数名 => 代入したい値]

### index.blade.phpの$todos・$todoに代入されているものは何か
todosテーブルの全レコード情報

## Todo作成機能

### Requestクラスのallメソッドは何をしているか
フォームから送信された値を個別ではなく一括で取得している

### fillメソッドは何をしているか
連想配列で取得した値をfillメソッドを使って各プロパティに一括で代入

### $fillableは何のために設定しているか
便利な一括代入には脆弱性があり、'$fillable'を適切に使用し制限をかけている。
対策せずに使用すると悪意あるユーザーに攻撃されてしまう恐れもある。

### saveメソッドで実行しているSQLは何か
オブジェクトの状態をDBに保存するINSERT文

### redirect()->route()は何をしているか
Todoが新規作成された後に、一覧画面にリダイレクトする処理

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
SQLを知らなくてもPHPコードでテーブル操作ができるため学習コストが不要
マイグレーションファイルをGITで共有することで
開発者全員が同じテーブルで作成できる

### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
up()　データベースに新しいテーブル、カラム、インデックスの追加に使用
down()　UPメソッドによって実行する操作と逆の操作を実装し、
　　　　以前の状態に戻す

### Seederクラスの役割は何か
開発中やテスト時に必要なデータを自動登録できるようにするために使う

### route関数の引数・返り値・使用するメリット
引数　第一引数がURI、第二引数そのURIとHTTPメソッド
返り値　ルート定義を表すオブジェクト
使用するメリット　実行したい処理を記述することでルートを定義することができる

### @extends・@section・@yieldの関係性とbladeを分割するメリット
@extends('Bladeファイルのパス')で継承する親bladeを指定し、
@section('任意の文字列') ~ @endsectionで囲った部分を継承したbladeファイルの
の@yield('任意の文字列')の箇所に挿入して紐付けている。
その結果、重複するコードを共通化しコードを再利用することで保守性が向上する

### @csrfは何のための記述か
CSRF対策のための記述

`CSRF対策とは、Todoアプリから返されるHTMLにワンタイムトークンを埋め込み
再度リクエストするときにワンタイムトークンも送信し
リクエスト元が正しいかチェックする`

### {{ }}とは何の省略系か
変数や関数をHTMLで出力する際に使用するXSS対策エスケープ文の省略
